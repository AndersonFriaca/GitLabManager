import requireDir from "require-dir";

// requireDir("./../../business", { recurse: true });
requireDir("./../../controllers", { recurse: true });
// requireDir("./../../facades", { recurse: true });
// requireDir("./../../transports", { recurse: true });
// requireDir("./../../services", { recurse: true });
requireDir("./../../middlewares", { recurse: true });
